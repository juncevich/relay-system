spring:
  datasource:
    url:
    username: 
    password:
    initialization-mode: embedded
  jpa:
    hibernate:
      ddl-auto: validate
    database: h2
    show-sql: true
    database-platform: 
