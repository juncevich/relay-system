apiVersion: v1
kind: Service
metadata:
  name: { { include "relay-service.fullname" . } }
  labels:
    { { - include "relay-service.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "relay-service.selectorLabels" . | nindent 4 } }
