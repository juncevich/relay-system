export class RelayModel {
  number: string;
  name: string;
  description: string;

  constructor(number: string, name: string, description: string) {
    this.number = number;
    this.name = name;
    this.description = description;
  }
}
