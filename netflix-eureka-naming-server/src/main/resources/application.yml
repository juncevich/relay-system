server:
  port: 8761

spring:
  application:
    name: netflix-eureka-naming-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka

