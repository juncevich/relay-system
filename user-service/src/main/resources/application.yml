server:
  port: 0
spring:
  application:
    name: user-service
  devtools:
    restart:
      enabled: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
